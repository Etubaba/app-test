import { View, Text, SafeAreaView } from "react-native";
import React from "react";

const modal = () => {
  return (
    <SafeAreaView>
      <Text>modal</Text>
    </SafeAreaView>
  );
};

export default modal;
